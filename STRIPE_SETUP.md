# 🎉 STRIPE ПОДКЛЮЧЁН! Осталось 2 шага

## ✅ У тебя есть ключи:
- ✅ Publishable key: `pk_test_YOUR_TEST_KEY`
- ✅ Secret key: `sk_test_YOUR_TEST_SECRET`

---

## 📋 ШАГ 1: Создай продукт в Stripe (1 минута)

1. **Зайди в твой Stripe Dashboard:** https://dashboard.stripe.com
2. **В левом меню нажми:** `Product catalog` (или `Products`)
3. **Нажми кнопку:** `+ Add product`
4. **Заполни форму:**
   ```
   Name: AI Learning Full Course Access
   Description: Lifetime access to all AI learning courses (16 levels)
   
   Price:
   - Amount: $100 (или $150, как хочешь)
   - Type: One time
   - Currency: USD
   ```
5. **Нажми:** `Save product`
6. **Скопируй Price ID** - он будет начинаться с `price_...`
   (Например: `price_1SQxp7EUse1J07rXabc123def456`)

---

## 📋 ШАГ 2: Создай файл .env.local

В корне проекта создай файл `.env.local` и вставь:

```bash
# Stripe Keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_TEST_KEY
STRIPE_SECRET_KEY=sk_test_YOUR_TEST_SECRET

# App Settings
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-random-secret-key-change-this-in-production
NODE_ENV=development
```

### Как создать файл:

**Вариант 1 - Терминал:**
```bash
cd /Users/maksymmotok/111
nano .env.local
# Вставь содержимое выше
# Ctrl+O для сохранения
# Ctrl+X для выхода
```

**Вариант 2 - VS Code:**
1. В корне проекта нажми правой кнопкой → `New File`
2. Назови файл `.env.local`
3. Вставь содержимое

**Вариант 3 - Терминал (быстро):**
```bash
cat > /Users/maksymmotok/111/.env.local << 'EOF'
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_TEST_KEY
STRIPE_SECRET_KEY=sk_test_YOUR_TEST_SECRET
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-random-secret-key-change-this
NODE_ENV=development
EOF
```

---

## 📋 ШАГ 3: Добавь Price ID в код

Когда получишь Price ID, открой файл:
`/Users/maksymmotok/111/app/payment/page.tsx`

Найди строку 58:
```typescript
priceId: 'price_1Oxxxxx', // Replace with your actual Stripe price ID
```

Замени на:
```typescript
priceId: 'price_ТВОЙ_НАСТОЯЩИЙ_ID', // Твой Price ID из Stripe
```

---

## ✅ ПРОВЕРЬ ЧТО ВСЁ РАБОТАЕТ

### 1. Перезапусти сервер:
```bash
# Останови текущий (Ctrl+C)
npm run dev
```

### 2. Открой сайт:
```
http://localhost:3000/payment
```

### 3. Попробуй оплату:
- Зарегистрируйся (если ещё не)
- Нажми "Оплатить"
- 💳 НАСТРОЙКА STRIPE ОПЛАТЫ - ПОЛНАЯ ИНСТРУКЦИЯ

## 🚨 КРИТИЧНО ДЛЯ РАБОТЫ ОПЛАТЫ!

---

## 📋 ШАГ ЗА ШАГОМ:

### **1️⃣ СОЗДАЙ STRIPE АККАУНТ**

1. Зайди на https://stripe.com
2. Нажми **Sign up** (Регистрация)
3. Создай аккаунт
4. Активируй аккаунт (добавь данные компании)

---

### **2️⃣ СОЗДАЙ ПРОДУКТ И ЦЕНУ**

1. Зайди на https://dashboard.stripe.com/products
2. Нажми **+ Add product** (Добавить продукт)
3. Заполни:
   - **Name:** AI Learning Platform - Full Access
   - **Description:** Lifetime access to all courses
   - **Pricing model:** One time (Единоразовая оплата)
   - **Price:** $100.00 USD
4. Нажми **Save product**
5. **СКОПИРУЙ PRICE ID** (формат: `price_1ABC...XYZ`)
   - Находится под ценой
   - Начинается с `price_`

---

### **3️⃣ ПОЛУЧИ API КЛЮЧИ**

1. Зайди на https://dashboard.stripe.com/apikeys
2. Найди секцию **Secret key**
3. Нажми **Reveal test key**
4. **СКОПИРУЙ Secret key** (формат: `sk_test_...`)
5. **СКОПИРУЙ Publishable key** (формат: `pk_test_...`)

**⚠️ ВАЖНО:**
- Для ТЕСТОВ используй `sk_test_...`
- Для ПРОДАКШНА используй `sk_live_...`

---

### **4️⃣ ДОБАВЬ ПЕРЕМЕННЫЕ В NETLIFY**

1. Открой https://app.netlify.com
2. Выбери свой сайт **ai-learning45**
3. Перейди: **Site settings** → **Environment variables**
4. Нажми **Add a variable**
5. Добавь **3 ПЕРЕМЕННЫЕ:**

**Переменная #1:**
```
Key: STRIPE_SECRET_KEY
Value: sk_test_ваш_ключ_здесь
```

**Переменная #2:**
```
Key: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY  
Value: pk_test_ваш_ключ_здесь
```

**Переменная #3:**
```
Key: NEXT_PUBLIC_STRIPE_PRICE_ID
Value: price_ваш_id_здесь
```

6. Нажми **Save** для каждой

---

### **5️⃣ НАСТРОЙ WEBHOOK (ДЛЯ ПОДТВЕРЖДЕНИЯ ОПЛАТ)**

1. Зайди на https://dashboard.stripe.com/webhooks
2. Нажми **+ Add endpoint**
3. Заполни:
   - **Endpoint URL:** `https://ai-learning45.netlify.app/api/webhook/stripe`
   - **Events to send:** Выбери `checkout.session.completed`
4. Нажми **Add endpoint**
5. **СКОПИРУЙ Signing secret** (формат: `whsec_...`)
6. Добавь в Netlify:
```
Key: STRIPE_WEBHOOK_SECRET
Value: whsec_ваш_секрет_здесь
```

---

### **6️⃣ ПЕРЕДЕПЛОЙ САЙТ**

**Способ A - Автоматически:**
- Сделай любой git push → автодеплой

**Способ B - Вручную:**
1. В Netlify: **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

**Подожди 2-3 минуты пока деплой закончится**

---

## ✅ ПРОВЕРКА ЧТО ВСЁ РАБОТАЕТ:

### **Тест оплаты:**

1. Открой https://ai-learning45.netlify.app/payment
2. Нажми **"Оплатить $100"**
3. Должна открыться **форма Stripe**
4. Используй **тестовую карту:**
   - Номер: `4242 4242 4242 4242`
   - CVC: любые 3 цифры (например `123`)
   - Дата: любая будущая (например `12/25`)
   - Имя: любое
5. Нажми **Pay**
6. Должен редирект на `/payment/success`
7. Открой `/courses` - должны быть доступны **ВСЕ курсы**

---

## 🎭 MOCK РЕЖИМ (БЕЗ STRIPE):

Если **НЕ настроил** Stripe, система работает в mock режиме:
- ✅ Оплата симулируется
- ✅ Доступ открывается БЕЗ реальной оплаты
- ⚠️ Показывает предупреждение

**🚨 НЕ ЗАПУСКАЙ В ПРОДАКШН БЕЗ НАСТРОЙКИ STRIPE!**
**Пользователи получат доступ бесплатно!**

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ:

### **Ошибка: "No such price"**
❌ **Причина:** Неправильный Price ID  
✅ **Решение:** 
1. Проверь `NEXT_PUBLIC_STRIPE_PRICE_ID` в Netlify
2. Скопируй правильный Price ID из Stripe Dashboard
3. Обнови переменную и передеплой

### **Ошибка: "Authentication failed"**
❌ **Причина:** Неправильный Secret key  
✅ **Решение:**
1. Проверь `STRIPE_SECRET_KEY` в Netlify
2. Скопируй правильный ключ из https://dashboard.stripe.com/apikeys
3. Убедись что используешь `sk_test_...` (не `pk_...`)

### **Оплата проходит, но доступ не открывается**
❌ **Причина:** Webhook не настроен  
✅ **Решение:**
1. Настрой webhook (шаг 5)
2. Проверь что `STRIPE_WEBHOOK_SECRET` добавлен
3. Проверь логи в https://dashboard.stripe.com/webhooks

### **Кнопка оплаты не работает (HTTP 500)**
❌ **Причина:** Переменные не добавлены или деплой не прошёл  
✅ **Решение:**
1. Проверь что ВСЕ 3 переменные добавлены в Netlify
2. Передеплой сайт
3. Открой консоль (F12) и посмотри ошибки

---

## 📊 ПРОВЕРКА НАСТРОЙКИ:

**Откройте консоль (F12) на странице `/payment`:**

**Правильно настроено:**
```
🔑 Stripe key present: true
🔑 Stripe key length: 107
🔑 Stripe key starts with: sk_test
✅ Creating Stripe checkout session...
✅ Stripe session created: cs_test_...
```

**Не настроено (mock mode):**
```
🔑 Stripe key present: false
⚠️ STRIPE_SECRET_KEY not configured - using MOCK mode
🎭 Returning mock session
```

---

## 🎯 БЫСТРЫЙ ЧЕКЛИСТ:

```
□ Создал Stripe аккаунт
□ Создал продукт ($100)
□ Скопировал Price ID (price_...)
□ Скопировал Secret key (sk_test_...)
□ Скопировал Publishable key (pk_test_...)
□ Добавил STRIPE_SECRET_KEY в Netlify
□ Добавил NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY в Netlify
□ Добавил NEXT_PUBLIC_STRIPE_PRICE_ID в Netlify
□ Настроил webhook
□ Добавил STRIPE_WEBHOOK_SECRET в Netlify
□ Передеплоил сайт
□ Протестировал оплату тестовой картой
```

---

## 💰 ПЕРЕХОД НА ПРОДАКШН:

**Когда готов принимать реальные деньги:**

1. Активируй аккаунт Stripe (добавь данные компании)
2. Переключись на **Live mode** в Dashboard
3. Создай **новый продукт** в Live mode (цена $100)
4. Скопируй **Live keys:**
   - Secret key (`sk_live_...`)
   - Publishable key (`pk_live_...`)
   - Price ID (`price_...` из live mode)
5. **ЗАМЕНИ** переменные в Netlify на Live keys
6. Настрой webhook для Live mode
7. Передеплой
8. **Протестируй с РЕАЛЬНОЙ картой!**

---

**ГОТОВО! ОПЛАТА ДОЛЖНА РАБОТАТЬ!** 💳✅

---

**Напиши когда:**
1. ✅ Создашь продукт и получишь Price ID
2. ✅ Создашь .env.local
3. ✅ Протестируешь оплату

И я помогу с остальным! 🚀
