# ⚡ ФИНАЛЬНАЯ НАСТРОЙКА - ВЫПОЛНИ ПО ПОРЯДКУ

## 🎯 ПРОБЛЕМЫ КОТОРЫЕ ИСПРАВЛЯЕМ:

1. ❌ В БД 211 старых уроков (нужно очистить)
2. ❌ DELETE не работает (RLS блокирует)
3. ❌ Счётчик уроков не обновляется автоматически

---

## 📋 ВЫПОЛНИ В SUPABASE SQL EDITOR ПО ПОРЯДКУ:

### **ШАГ 1: Настройка 4 курсов** (если ещё не сделал)
```sql
-- Запусти файл: SETUP_4_COURSES.sql
-- Это создаст таблицу course_categories и добавит колонку
```

### **ШАГ 2: Исправление RLS (чтобы DELETE работал)**
```sql
-- Запусти файл: FIX_DELETE_RLS.sql
-- Это исправит права доступа
```

### **ШАГ 3: Автообновление счётчика уроков**
```sql
-- Запусти файл: AUTO_UPDATE_LESSONS_COUNT.sql
-- Теперь при добавлении/удалении урока счётчик обновится сам
```

### **ШАГ 4: Очистка старых 211 уроков**
```sql
-- Запусти файл: CLEAR_OLD_LESSONS.sql
-- Удалит старые уроки, оставит только 14 последних
```

---

## ✅ РЕЗУЛЬТАТ:

После выполнения всех шагов:

### **1. Счётчик работает автоматически:**
```
Добавил урок в админке → +1 в базе
Удалил урок в админке → -1 в базе
```

### **2. DELETE работает:**
```
Кнопка "Удалить" в админке → урок удаляется из БД
```

### **3. Чистая база:**
```
Только актуальные уроки (14 штук)
Все 211 старых удалены
```

### **4. Привязка к курсам:**
```
Все 14 уроков привязаны к курсу "Сайты"
Счётчик показывает 14
```

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ:

1. Открой **Supabase Dashboard → SQL Editor**
2. Создай **New Query**
3. Выполни файлы **ПО ПОРЯДКУ**:
   - ✅ `SETUP_4_COURSES.sql`
   - ✅ `FIX_DELETE_RLS.sql`
   - ✅ `AUTO_UPDATE_LESSONS_COUNT.sql`
   - ✅ `CLEAR_OLD_LESSONS.sql`

---

## 🧪 ПРОВЕРКА:

### После выполнения всех SQL:

1. **Проверь количество уроков:**
```sql
SELECT COUNT(*) FROM courses;
-- Должно быть 14 (или сколько у тебя реально)
```

2. **Проверь счётчики:**
```sql
SELECT id, title, total_lessons FROM course_categories ORDER BY display_order;
-- Должно быть:
-- 1 | Сайты | 14
-- 2 | Приложения | 0
-- 3 | Игры | 0
-- 4 | Платёжные системы | 0
```

3. **Проверь админку:**
   - Зайди в `/admin`
   - Удали любой урок
   - Обнови страницу
   - Урок должен исчезнуть ✅

4. **Проверь фронт:**
   - Открой `/courses`
   - Увидишь 4 карточки курсов
   - Клик на "Сайты" → 14 уроков ✅

---

## ⚠️ ВАЖНО:

- Выполняй файлы **ПО ПОРЯДКУ**
- Не пропускай шаги
- После каждого файла смотри что нет ошибок

---

## 🎉 ГОТОВО!

После всех шагов:
- ✅ Счётчик обновляется автоматически
- ✅ DELETE работает
- ✅ База чистая (14 уроков)
- ✅ Все привязаны к курсу "Сайты"
- ✅ Админка работает правильно

**ВЫПОЛНИ ВСЕ 4 ФАЙЛА ПО ПОРЯДКУ!** 🚀
