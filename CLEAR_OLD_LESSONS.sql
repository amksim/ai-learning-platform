-- 🗑️ ОЧИСТКА СТАРЫХ УРОКОВ
-- Удаляет все 211 старых уроков из БД

-- ВАРИАНТ 1: УДАЛИТЬ ВСЕ (если хочешь начать с нуля)
-- DELETE FROM courses;

-- ВАРИАНТ 2: ОСТАВИТЬ ТОЛЬКО 14 ПОСЛЕДНИХ (твои рабочие уроки)
-- Удаляем все кроме последних 14 по ID
DELETE FROM courses 
WHERE id NOT IN (
  SELECT id 
  FROM courses 
  ORDER BY id DESC 
  LIMIT 14
);

-- Проверяем результат
SELECT COUNT(*) as total_lessons FROM courses;
SELECT id, title FROM courses ORDER BY id;
